CSRF(跨站点请求伪造)
用户通过修改请求获取他不应该获取的数据，
如获取本人记录的请求是 /getuserinfo.php?id=23
完全可以在请求包中把id改为其他用户的，同时包括cookie 源URL都是可以修改的

可以通过
1.要求以SSL连接来问方可以通过XHR请求的资源
//SSL是一种加密算法，类型是服务器给客户端发送数据，客户端根据数据生成主密钥，并用服务器公钥对其进行加密返回给服务器
服务器回复该密钥，并返回给客户端一个用主密钥认证的信息
2.要求每一次请求都要附带经过计算的验证码
